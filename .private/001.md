# Create
npx create-next-app@latest --app --src-dir genkit-nextjs
cd genkit-nextjs
# Install
npm install genkit @genkit-ai/google-genai @genkit-ai/next
npm install -D genkit-cli tsx
# Environment 
touch .env
echo "GEMINI_API_KEY=SOME_KEY" >> .env
- - - - - 
# Create a route inside src/app/api/multiplier/route.ts

- - -
RUN
npm run dev
 - - - - DB
npm install drizzle-orm pg
npm install -D drizzle-kit @types/pg dotenv
Create drizzle.config.ts
```
import { defineConfig } from 'drizzle-kit';
import 'dotenv/config';

export default defineConfig({
  schema: './src/db/schema.ts',
  out: './src/db/migrations',
  dialect: 'postgresql',
  dbCredentials: {
    url: process.env.DATABASE_URL!,
  },
});

```
Add scripts to package.json
```
"scripts": {
  "db:generate": "drizzle-kit generate",
  "db:migrate": "drizzle-kit migrate",
  "db:studio": "drizzle-kit studio"
}

```
For prod deployment
 "build": "drizzle-kit migrate && next build"
 --
 Create your database schema in src/db/schema.ts
 ```
 import { pgTable, serial, text, timestamp } from "drizzle-orm/pg-core";
export const users = pgTable("order-status", {
  id: serial("id").primaryKey(),
  product: text("product").notNull(),
  status: text("status").unique().notNull(),
  createdAt: timestamp("created_at").defaultNow(),
});

 ```
 Generate
 npx drizzle-kit generate
 This will create
 src/db/migrations/0000_jazzy_pet_avengers.sql
Migrate
npx drizzle-kit migrate
- - - -
Install
npm i drizzle-seed
npm i -D tsx
Create a file at src/db/seed.ts
```
```
Add Command to package.json
```
{
  "scripts": {
    "db:seed": "tsx --env-file=.env src/db/seed.ts"
  }
}
```
Run
npm run db:seed
--- CRUD
Push schema
npx drizzle-kit push

